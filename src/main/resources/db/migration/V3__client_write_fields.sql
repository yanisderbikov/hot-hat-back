-- Поля, которые пишет клиент, но которых не оказалось в схеме.
--
-- clock_probe_at: игрок пишет туда серверное время и сразу читает обратно,
-- чтобы измерить расхождение своих часов с сервером. Без него у игроков
-- разъезжается отсчёт хода.

ALTER TABLE room_player ADD COLUMN clock_probe_at TIMESTAMPTZ;
