package ru.hothat.rating.usecase;

/**
 * Числа, которые область рейтинга обязана называть одинаково в базе и в
 * ответе.
 *
 * <p>Перенесены из старого движка ({@code RatingServiceImpl}) без изменений:
 * переезд на v2 не должен менять то, что видит игрок.
 */
public final class RatingReadLimits {

    /**
     * Сколько друзей просматривается ради признака «в команде есть друг».
     *
     * <p>Столько же брал старый движок. Признак приблизителен по устройству:
     * у игрока с сотней друзей сто первый в подсветку не попадёт — и это
     * лучше, чем читать весь список ради одной галочки на строке.
     */
    public static final int FRIENDS_SCAN = 100;

    private RatingReadLimits() {
    }
}
