package ru.hothat.game.api.dto;

import io.swagger.v3.oas.annotations.media.Schema;

/**
 * Игрок сообщил, что уходит.
 *
 * <p>Отдельный адрес от сверки присутствия и от разрыва связи по трём
 * причинам. Он приходит на закрытии вкладки, когда браузер даёт одну попытку
 * и никакого ответа уже не прочитает. Он никогда не закрывает комнату — уход
 * одного человека не повод стирать чужую партию. И он ничего не спрашивает у
 * сервера видеосвязи: тот отдаёт ушедшего ещё несколько секунд, а отметку надо
 * погасить сейчас.
 */
@Schema(description = "Отметка об уходе принята")
public record MatchDepartureResponseDTO(

        @Schema(description = "Партия остановлена этим уходом", example = "true")
        boolean paused,

        @Schema(description = "Состояние паузы")
        PauseView pause) {
}
