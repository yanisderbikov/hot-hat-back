package ru.hothat.game.domain.weapon;

/**
 * Обработчик одного вида оружия.
 *
 * <p>Реестр обработчиков вместо тринадцати адресов — решение заказчика
 * (вопрос 2 плана). Общие правила — фаза, принадлежность к партии,
 * перезарядка, боезапас, занятость дорожки — проверены до вызова, поэтому
 * обработчику остаётся только своё: мем проверяет обойму, Подмена — клип,
 * накладка — координаты.
 */
@FunctionalInterface
public interface WeaponHandler {

    SabotageDecision fire(SabotageContext context);
}
