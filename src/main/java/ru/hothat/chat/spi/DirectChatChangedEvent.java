package ru.hothat.chat.spi;

/**
 * В личной переписке появилось сообщение.
 *
 * <p>По этому событию именованный канал {@code /ws/v2/chat/{peerUid}}
 * рассылает собеседникам новый снимок переписки. Событие, а не прямой вызов
 * рассылки из сценария: рассылка вправе опоздать и не должна иметь возможности
 * уронить запись сообщения (§7.3 плана).
 *
 * <p>Живёт у владельца переписки. Публикуют его отправка текста, фотографии,
 * поделённой записи и приглашения в комнату — последнее через
 * {@link ChatCommandPort}, потому что заводит его область комнаты, а строку
 * переписки пишет всё равно эта.
 *
 * @param pair идентификатор пары собеседников — тот же {@code Ids.pair(a, b)},
 *             которым переписка адресуется в хранилище
 */
public record DirectChatChangedEvent(String pair) {
}
