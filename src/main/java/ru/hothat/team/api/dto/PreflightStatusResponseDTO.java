package ru.hothat.team.api.dto;

import io.swagger.v3.oas.annotations.media.Schema;

/**
 * Текущая проверка готовности пары — или её отсутствие.
 *
 * <p>Адрес опрашивают оба участника, пока идёт подготовка: напарник видит
 * чужие флаги связи и готовности только отсюда.
 *
 * <p>Отсутствие проверки — не ошибка и не пустой ответ 404: у пары, которая
 * просто открыла свою страницу, проверки нет, и это нормальное состояние. Одна
 * форма ответа, отсутствие выражено пустым полем.
 */
@Schema(description = "Состояние проверки готовности")
public record PreflightStatusResponseDTO(

        @Schema(description = "Снимок проверки; null — проверка не начата или уже протухла")
        PreflightSessionView session) {
}
