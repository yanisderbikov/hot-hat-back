package ru.hothat.team.api.dto;

import io.swagger.v3.oas.annotations.media.Schema;

/**
 * Только что созданная проверка готовности.
 *
 * <p>Своя пара к операции старта, а не общая с чтением: у чтения снимка может
 * не быть вовсе, а у только что созданной проверки он есть всегда — поэтому
 * поле здесь не допускает null, и клиенту не нужно проверять его наличие
 * сразу после успешного создания.
 *
 * <p>Старый ответ приезжал плоским: {@code ok:true} и рядом двадцать один ключ
 * самой проверки. По нему нельзя было отличить «проверка создана» от «проверка
 * не создалась, но действие прошло» — ровно этот случай движок и возвращал,
 * когда снимок оказывался пустым.
 */
@Schema(description = "Созданная проверка готовности")
public record PreflightSessionResponseDTO(

        @Schema(description = "Снимок созданной проверки")
        PreflightSessionView session) {
}
